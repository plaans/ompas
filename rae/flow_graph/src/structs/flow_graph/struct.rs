use crate::structs::chronicle::atom::Atom;
use crate::structs::chronicle::interval::Interval;
use crate::structs::chronicle::lit::Lit;
use crate::structs::chronicle::AtomId;
use crate::structs::flow_graph::expression::IfBlock;
/*
pub struct Prime {
    interval: Interval,
    result: AtomId,
    expression: ExprId,
}

pub struct Expression {
    id: ExprId,
    kind: ExpressionKind,
}

pub enum ExpressionKind {
    Cst(AtomId),
    Lit(Lit),
    Apply(Vec<AtomId>),
    Handle(AtomId),
}

pub type BlockId = usize;

pub struct Block {
    id: BlockId,
    inner: Vec<BlockInner>,
}

pub enum BlockInner {
    If(IfBlock),
}*/
