/*pub enum LValueType {
    Int,
    Float,
    Symbol,
    String,
    Err(LValueType),
    Map(LValueType, LValueType),
    Tuple(Vec<LValueType>),
    Union(Vec<LValueType>),
    List(LValueType),
    Function(LValueType, LValueType),
}*/
